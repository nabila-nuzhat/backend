---------- API Design -----

- API 
    - is API only a set of rules ???
    - is API a URL ??

- how API works ??
- Types of API ??
    -  - SOAP APIs
    - _RPC 
        - _RPC API is used for payment ??
    - REST API
    - Web Socket
        - eg. messenger, insta post seen by user list ??

- XML 
    - xml data type??

- REST API 
- REST API Principles and Constrains - 6
    - Client-Server
        - client server should be sepaerate...not tight coupled. ??
    - Ceachability ?? 
        - using header 
        - non-cacheable eg. authentication ??
    - Uniform Interface
        - consistency of URL, so that URL structure is manitained
        - no singular URL (eg. NOT user in URL. "users" in URL)
    - Layered System
        - Architecture composed of multiple layers (proxies, gateways). list of the layers ??
        - REST API Each layer only knows about the adjacent layer means ??
            - will be cleared while NESTING ???
            - adjucent layer ceachable ??
    - Code on Demand (optional)
        - eg. a file ins needed in a fix url

    - An API that follows these constraints is called "RESTful" and operates over HTTP. ??

- HTTP status code in REST archiectecture

- ------------------------------------ API DESIGN PRACTICES -------------------------------------------------------
- API Versioning Strategies
    - Versioning should be- 
    - new feature added... API change in current API will cause crash, so for newer change new version of API should be added. so that 2 apps can run the versions.
    - eg. some new features of fb/wapp only available in newer android phones/ iphones
    
    - API Versioning Strategies: 3 ways
        - URL-Based
        - Header-Based
            - cons:
                - apps doesnt support cookies only supported by browsers so, it's easier using URL-Based
                Versioning of API.
        - Query Parameter
            - not good, it will be visible as ? query. but URL-Based will NOT be visible to user. thus cannot manipulate ???

- URL Structure & Pagination: --------------------------
- URL DESIGN RULES
    - Avoid deep nesting (nesting better in database)
- PAGINATION STRATEGIES
    - Offset-Based Pagination
        - eg. not usually an issue in smaller scale e-commerce like startech, sajgoj
    - Cursor-Based Pagination
        - eg. large scale like amazon
        - to skip certain limit...the ids will be set which ones to skip like last id predict and calculate
        - cursor = user id ??



