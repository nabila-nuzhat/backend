---------- API Design -----

- API 
    - is API only a set of rules ???
    - is API a URL ??

- how API works ??
- Types of API ??
    -  - SOAP APIs
    - _RPC 
        - _RPC API is used for payment ??
    - REST API
    - Web Socket
        - eg. messenger, insta post seen by user list ??

- XML 
    - xml data type??

- REST API 
- REST API Principles and Constrains - 6
    - Client-Server
        - client server should be sepaerate...not tight coupled. ??
    - Ceachability ?? 
        - using header 
        - non-cacheable eg. authentication ??
    - Uniform Interface
        - consistency of URL, so that URL structure is manitained
        - no singular URL (eg. NOT user in URL. "users" in URL)
    - Layered System
        - Architecture composed of multiple layers (proxies, gateways). list of the layers ??
        - REST API Each layer only knows about the adjacent layer means ??
            - will be cleared while NESTING ???
            - adjucent layer ceachable ??
    - Code on Demand (optional)
        - eg. a file ins needed in a fix url

    - An API that follows these constraints is called "RESTful" and operates over HTTP. ??

- HTTP status code in REST archiectecture

- ------------------------------------ API DESIGN PRACTICES -------------------------------------------------------
- API Versioning Strategies
    - Versioning should be- 
    - new feature added... API change in current API will cause crash, so for newer change new version of API should be added. so that 2 apps can run the versions.
    - eg. some new features of fb/wapp only available in newer android phones/ iphones
    
    - API Versioning Strategies: 3 ways
        - URL-Based
        - Header-Based
            - cons:
                - apps doesnt support cookies only supported by browsers so, it's easier using URL-Based
                Versioning of API.
        - Query Parameter
            - not good, it will be visible as ? query. but URL-Based will NOT be visible to user. thus cannot manipulate ???

- URL Structure & Pagination: --------------------------
- URL DESIGN RULES
    - Avoid deep nesting (nesting better in database)
- PAGINATION STRATEGIES
    - Offset-Based Pagination
        - eg. not usually an issue in smaller scale e-commerce like startech, sajgoj
    - Cursor-Based Pagination
        - eg. large scale like amazon
        - to skip certain limit...the ids will be set which ones to skip like last id predict and calculate
        - cursor = user id ??

- API Authentication & Security
    - API Keys
        - Best for: Public APIs, rate limiting ??
        - Limitation: No user identity: server only knows the key cannot identify if the key is provided by the exact authentic user, 
            - easy to leak

    - OAuth 2.0
        - eg. login with google, login with gitgub etc.
        - Users grant limited access without sharing passwords .... how without sharing passwords ??
        - Delegated authorization framework ??

    - JWT Tokens
        - Format: Header.Payload.Signature
            (Algorithm (Hashing algorithm), Payload, Signature (sir point))
        - Best for: Stateless auth, microservices ??
        - Resfresh token ??

    - Security BEST Practices


 - Hashing : 
    - hiding data. 
    - once hashed cannot be retreived original format, but key is used to verify.
    - used in crypto, banking
    - hashing happens on OS level, so to retreive original format has to engage OS/CPU operation
- Encryption:
    - 2 Keys
    - can be retreived decrypted
    - have to know algorithm to deep diving

- Crypto:
    - mining happens on OS/CPU level

- Rate Limiting:
    - eg. limited tries of password input.
    - limit: banned for limited time/ forever
        or, slow down 

- DDos:
    - at a time too many user

- API Documentation with OpenAPI ??
    - eg. dummy.json gives a documentation so that dev can use it by going through the documentation.
    - Multilanguage documentation is always preferred as devs from different language
    - OpenAPI (formerly Swagger) is a machine-readable format for describing REST APIs in YAML or JSON. ?? 
        - Swagger : a package
        follows a standard that is OpenAPI.

    - Server Stubs ??
        - Auto-generate backend code....in API Documentation what??
        - SDKs for multiple languages... SDK ??
    - Interactive Docs ??
        - Swagger UI for exploration ?? 
    - Auto Testing ??
        - create workflows for auto testing ??

    - 


- YAML ??
    - data type

- WHY USE OPENAPI?

- Validation, Optimization & Errors --------------------------
- RESPONSE OPTIMIZATION
    - RESPONSE OPTIMIZATION of REST API design ??
    - Pagination
        Return subsets instead of all data
    - Compression
        Use Gzip or Brotli to minimize payload ??
    - Caching
        Store frequently accessed data
    - Serialization
        Use lightweight formats like MessagePack ??

- ERROR HANDLING
    - Clear Messages
        - be specific relevant error message. 
        but still not always very direct that will subject to info leak/hacking targetted weak point. ??
        
        - usually packege used for automated hangdling. ??

- SQL injectuion ??
    - that runs a script on db ??

- Stack traces:
    - means locate the exact file where error occured.
    should not be exposed to client.
    - will only stay in develoment phase.
    will be off when the app goes to production.




