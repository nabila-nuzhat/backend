- Buffer methods

-  Buffer.alloc()
    - by default data is in UTF-8 format
    - - why Buffer.alloc() Zero-Filled & Secure?
    - (why Bufferaloc Zero-Filled & Secure - google - ss)

- Buffer.from()
    - (convert string to hex using Buffer from - google - ss)
    ************ (Buffer from Encoding Deep Dive- google AI - google -ss)*********
    ************ (Buffer From Encoding.docx) ************************************

    - Encoding vs Conversion vs Decoding in buffer.from()
        (Encoding vs Conversion vs Decoding in buffer from – google -ss)
        (Buffer From Encoding.docx)

- how to find out what is base64 format of a string in buffer?
   ( how to find out what is base64 format of a string in buffer - google - ss)

- Base64 increases data size by approximately 33%. ??? LATERRRRRRRRR


- Read buffer
    - toString() - whole string
    - buf.toString([encoding], [start], [end]) (partial)
 
- Write Buffer
    - buff.write() and toString() to see how much is stored
    - buf.write(string, [offset], [length], [encoding])
        not always needed

- buff.write()

- if Buffer.from() stores a string in buffer memory why buff.write() is needed?
    (Buffer from Vs buff write method – google -ss)
    (Buffer Methods.docx)


- Buffer toString() method_Buffer vs buffer string
    (Buffer toString method_Buffer vs buffer string - claude - ss)


